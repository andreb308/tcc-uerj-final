generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "mysql"
}

model Report {
  id             String       @id @default(cuid())
  userId         String       @default("legacy-user")
  status         ReportStatus @default(pending)
  artist         String
  trackTitle     String
  targetLanguage String
  artifactData   String       @db.Text
  albumCover     Json?
  reportData     Json?
  chatHistory    Json?
  createdAt      DateTime     @default(now())

  @@map("reports")
}

enum ReportStatus {
  pending
  generating
  complete
  error
}
